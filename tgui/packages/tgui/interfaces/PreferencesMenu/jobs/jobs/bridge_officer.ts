import { Job } from "../base";
import { Command } from "../departments";

const BridgeOfficer: Job = {
  name: "Bridge Officer",
  description: "File paperwork, pretend to be a head of staff, be ignored.",
  department: Command,
};

export default BridgeOfficer;
