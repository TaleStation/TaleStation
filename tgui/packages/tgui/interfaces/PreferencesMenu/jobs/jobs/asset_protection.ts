import { Job } from "../base";
import { Command } from "../departments";

const AssetProtection: Job = {
  name: "Asset Protection",
  description: "Protect heads of staff and the captain from an untimely \
    demise, exert authority over the Bridge Officer.",
  department: Command,
};

export default AssetProtection;
