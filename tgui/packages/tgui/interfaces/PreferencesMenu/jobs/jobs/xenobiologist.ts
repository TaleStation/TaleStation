import { Job } from "../base";
import { Science } from "../departments";

const Xenobiologist: Job = {
  name: "Xenobiologist",
  description: "Feed slimes the entire shift, never exit xenobiology. \
    Finally leave after two hours as an unkillable god with an army.",
  department: Science,
};

export default Xenobiologist;
